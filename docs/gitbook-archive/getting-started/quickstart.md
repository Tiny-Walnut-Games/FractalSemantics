# Getting Started

This is the GitBook-archive quick entry page.

For the complete up-to-date quickstart flow, use:

- `QUICKSTART.md`
- `README.md`

Recommended first commands:

```bash
pip install -r requirements.txt
pip install -e .
python fractalsemantics/experiment_runner.py --all --quick --format=text
python comprehensive_experiment_analysis.py
```

For full public CLI/API scope, use the pages in this archive and the root README.
